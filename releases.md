# @ouroboros/blog releases

## 0.2.0
- Updated `@ouroboros/body` to latest.
- Removed the no longer necessay xhr.d.ts file from the project.
- Removed local "definition" files and replaced them with submodule "define" files.